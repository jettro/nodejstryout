h1 My blog
div
    a(href='/blog/new') New blog item
div#articles
    - each blog in docs
        div.article
            div.title #{blog.title}
            div.created_at #{blog.created_at}
            div.body #{blog.body}
div#helloworld
    span Enter a message to be echoed
    input(type="text", name="new_hello", size="50")
    input(type="button", value="Echo", onClick="javascript:socket.send('Check what I can do')")